import ParentIdentitySection from '../../components/sections/parentLogin/ParentIdentitySection.jsx';

/*
 * ParentIdentityPage — Step 1 of parent sign-up wizard.
 * Route: /onboarding/parent-identity (inside ParentOnboardingLayout).
 * All layout (breadcrumb, bg glows, right panel) is provided by the layout shell.
 */
const ParentIdentityPage = () => <ParentIdentitySection />;

export default ParentIdentityPage;
