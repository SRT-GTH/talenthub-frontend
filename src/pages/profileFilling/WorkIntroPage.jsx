import WorkIntroSection from '../../components/sections/profileFilling/WorkIntroSection.jsx';

/*
 * WorkIntroPage — route endpoint for /profile/filling/work.
 * Thin wrapper; all chrome + content lives in WorkIntroSection.
 */
const WorkIntroPage = () => <WorkIntroSection />;

export default WorkIntroPage;
