import GoalsIntroSection from '../../components/sections/profileFilling/GoalsIntroSection.jsx';

/*
 * GoalsIntroPage — route endpoint for /profile/filling/goals.
 * Thin wrapper; all chrome + content lives in GoalsIntroSection.
 */
const GoalsIntroPage = () => <GoalsIntroSection />;

export default GoalsIntroPage;
