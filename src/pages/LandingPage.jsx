import { debug } from '../utils/debug.js';

const log = debug('LandingPage');

/*
 * LandingPage — public marketing page.
 * HeroSection import was removed while that component is being rebuilt.
 * Restore the section imports as their Figma frames land.
 */

const LandingPage = () => {
  log('mount');
  return null;
};

export default LandingPage;
