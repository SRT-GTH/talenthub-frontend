/**
 * Tailwind v4 reads its theme from CSS (@theme block in src/index.css).
 * This file exists only so editor tooling and any tooling that expects a
 * config file can find one. The real design tokens live in src/index.css.
 */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
};
